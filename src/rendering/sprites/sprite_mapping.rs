#![allow(dead_code)]
#[derive(Copy, Clone, Debug)]

/**
 * These need to be in the same order as loaded in load_level_sprites
 */
pub(crate) enum Sprite {
    DwarfBaseHouse,
    DwarfBaseHousePixelated,
    Black,
    DarkBlue,
    Green,
    WorkerBasic,
    BaseBaseLarge,
} 